# Moved to the rules.mk of the keymap files (outside of userspace), left this block here incase combo.c moves from "INTROSPECTION_KEYMAP_C"
# ifeq ($(strip $(USE_ALL_COMMON_COMBOS_COLEMAK_DHE)), yes)
#     SRC += combo.c
# endif